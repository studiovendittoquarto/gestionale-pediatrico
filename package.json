{
  "name": "gestionale-pediatrico",
  "version": "1.0.0",
  "description": "Gestionale appuntamenti pediatrico con real-time (Express + Socket.io + Postgres + React)",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev:server": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "postinstall": "echo Backend dependencies installed"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0",
    "socket.io": "^4.7.5"
  }
}
